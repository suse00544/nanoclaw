server {
    listen 443 ssl http2;
    server_name apple-code.example.com;

    # Configure your existing TLS certificate here.
    ssl_certificate /etc/letsencrypt/live/apple-code.example.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/apple-code.example.com/privkey.pem;

    # These endpoints contain secret tokens. Do not write their query strings or
    # request bodies to access logs.
    access_log off;

    location / {
        proxy_pass http://127.0.0.1:8791;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-Proto https;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        client_max_body_size 8k;
    }
}
